---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
          text: "unitTest(Test-1): bytes('abc') == bytes('abc')"
        arg:
          IS_EQUAL:
            left:
              BYTES: "abc"
            right:
              BYTES: "abc"
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): bytes('a b c') != bytes(' a   b  c ')"
        arg:
          NOT_EQUAL:
            left:
              BYTES: "a b c"
            right:
              BYTES: " a   b  c "
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): bytes('ABC') != bytes('abc')"
        arg:
          NOT_EQUAL:
            left:
              BYTES: "ABC"
            right:
              BYTES: "abc"
  - status: COMPLIANT
    currentStateMessage: Test-4
    check:
      UNIT_TEST:
        id: Test-4
        expected:
          text: "unitTest(Test-4): bytes('') == null"
        arg:
          IS_EQUAL:
            left:
              BYTES: ""
            right:
              UNIT_TEST_NULL:
                returnType: BYTES
  - status: COMPLIANT
    currentStateMessage: Test-5
    check:
      UNIT_TEST:
        id: Test-5
        expected:
          text: "unitTest(Test-5): bytes('') != bytes('\\n\\r\\t')"
        arg:
          NOT_EQUAL:
            left:
              BYTES: ""
            right:
              BYTES: "\n\r\t"
  - status: COMPLIANT
    currentStateMessage: Test-6
    check:
      UNIT_TEST:
        id: Test-6
        expected:
          text: "unitTest(Test-6): bytes('a b c') != bytes('  A\\n B \\r C \\t')"
        arg:
          NOT_EQUAL:
            left:
              BYTES: "a b c"
            right:
              BYTES: "  A\n B \r C \t"
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required