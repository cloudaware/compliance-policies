---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
          text: "unitTest(Test-1): setOfText(['a', 'b', 'c']) == setOfText(['a', 'b', 'c'])"
        arg:
          IS_EQUAL:
            left:
              SET:
                itemType: TEXT
                items:
                  - a
                  - b
                  - c
            right:
              SET:
                itemType: TEXT
                items:
                  - a
                  - b
                  - c
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): setOfText(['a', 'b', 'c']) == setOfText(['c', 'a', 'b'])"
        arg:
          IS_EQUAL:
            left:
              SET:
                itemType: TEXT
                items:
                  - a
                  - b
                  - c
            right:
              SET:
                itemType: TEXT
                items:
                  - c
                  - a
                  - b
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): setOfText(['a', 'b', 'c']) == setOfText(['a', 'a', 'b', 'b', 'c', 'c'])"
        arg:
          IS_EQUAL:
            left:
              SET:
                itemType: TEXT
                items:
                  - a
                  - b
                  - c
            right:
              SET:
                itemType: TEXT
                items:
                  - a
                  - a
                  - b
                  - b
                  - c
                  - c
  - status: COMPLIANT
    currentStateMessage: Test-4
    check:
      UNIT_TEST:
        id: Test-4
        expected:
          text: "unitTest(Test-4): setOfText(['a', 'b', 'c']) == setOfText(['a ', ' b ', ' c'])"
        arg:
          IS_EQUAL:
            left:
              SET:
                itemType: TEXT
                items:
                  - a
                  - b
                  - c
            right:
              SET:
                itemType: TEXT
                items:
                  - "a "
                  - " b "
                  - " c"
  - status: COMPLIANT
    currentStateMessage: Test-5
    check:
      UNIT_TEST:
        id: Test-5
        expected:
          text: "unitTest(Test-5): setOfText(['a', 'b', 'c']) == setOfText(['A', 'B', 'C'])"
        arg:
          IS_EQUAL:
            left:
              SET:
                itemType: TEXT
                items:
                  - a
                  - b
                  - c
            right:
              SET:
                itemType: TEXT
                items:
                  - A
                  - B
                  - C
  - status: COMPLIANT
    currentStateMessage: Test-6
    check:
      UNIT_TEST:
        id: Test-6
        expected:
          text: "unitTest(Test-6): setOfText([]) == setOfText([])"
        arg:
          IS_EQUAL:
            left:
              SET:
                itemType: TEXT
                items: []
            right:
              SET:
                itemType: TEXT
                items: []
  - status: COMPLIANT
    currentStateMessage: Test-7
    check:
      UNIT_TEST:
        id: Test-7
        expected:
          text: "unitTest(Test-7): null == null"
        arg:
          IS_EQUAL:
            left:
              UNIT_TEST_NULL:
                returnType: SET_OF_TEXT
            right:
              UNIT_TEST_NULL:
                returnType: SET_OF_TEXT
  - status: COMPLIANT
    currentStateMessage: Test-8
    check:
      UNIT_TEST:
        id: Test-8
        expected:
          text: "unitTest(Test-8): setOfText([]) == null"
        arg:
          IS_EQUAL:
            left:
              SET:
                itemType: TEXT
                items: []
            right:
              UNIT_TEST_NULL:
                returnType: SET_OF_TEXT
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required