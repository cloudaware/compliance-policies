---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
          text: "unitTest(Test-1): true == true"
        arg:
          IS_EQUAL:
            left:
              BOOLEAN: true
            right:
              BOOLEAN: true
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): true != false"
        arg:
          NOT_EQUAL:
            left:
              BOOLEAN: true
            right:
              BOOLEAN: false
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): false != null"
        arg:
          NOT_EQUAL:
            left:
              BOOLEAN: false
            right:
              UNIT_TEST_NULL:
                returnType: BOOLEAN
  - status: COMPLIANT
    currentStateMessage: Test-4
    check:
      UNIT_TEST:
        id: Test-4
        expected:
          text: "unitTest(Test-4): true != null"
        arg:
          NOT_EQUAL:
            left:
              BOOLEAN: true
            right:
              UNIT_TEST_NULL:
                returnType: BOOLEAN
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required