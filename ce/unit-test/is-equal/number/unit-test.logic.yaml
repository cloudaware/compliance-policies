---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
          text: "unitTest(Test-1): number(10.0) == number(10.0)"
        arg:
          IS_EQUAL:
            left:
              NUMBER: 10
            right:
              NUMBER: 10.0
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): number(10.0) != number(1.0)"
        arg:
          NOT_EQUAL:
            left:
              NUMBER: 10.0
            right:
              NUMBER: 1.0
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): number(0.0) != null"
        arg:
          NOT_EQUAL:
            left:
              NUMBER: 0
            right:
              UNIT_TEST_NULL:
                returnType: NUMBER
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required