---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
           text: "unitTest(Test-1): null.size() == number(0.0)"
        arg:
          IS_EQUAL:
            left:
              COLLECTION_SIZE:
                arg:
                  UNIT_TEST_NULL:
                    returnType: COLLECTION_TEXT
            right:
              NUMBER: 0
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): [].skipEmpty().skipDuplicates().sort().size() == number(0.0)"
        arg:
          IS_EQUAL:
            left:
              COLLECTION_SIZE:
                arg:
                  COLLECTION:
                    emptyValues: SKIP
                    duplicateValues: SKIP
                    ordering: SORTED
                    itemType: TEXT
                    items: []
            right:
              NUMBER: 0
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): ['a', 'b', 'c'].skipEmpty().skipDuplicates().sort().size() == number(3.0)"
        arg:
          IS_EQUAL:
            left:
              COLLECTION_SIZE:
                arg:
                  COLLECTION:
                    emptyValues: SKIP
                    duplicateValues: SKIP
                    ordering: SORTED
                    itemType: TEXT
                    items:
                      - a
                      - b
                      - c
            right:
              NUMBER: 3
  - status: COMPLIANT
    currentStateMessage: Test-4
    check:
      UNIT_TEST:
        id: Test-4
        expected:
          text: "unitTest(Test-4): ['a', 'a', 'a'].skipEmpty().skipDuplicates().sort().size() == number(1.0)"
        arg:
          IS_EQUAL:
            left:
              COLLECTION_SIZE:
                arg:
                  COLLECTION:
                    emptyValues: SKIP
                    duplicateValues: SKIP
                    ordering: SORTED
                    itemType: TEXT
                    items:
                      - a
                      - a
                      - a
            right:
              NUMBER: 1
  - status: COMPLIANT
    currentStateMessage: Test-5
    check:
      UNIT_TEST:
        id: Test-5
        expected:
          text: "unitTest(Test-5): ['a', 'A', ' A ', '\\ta\\n\\r'].skipEmpty().skipDuplicates().sort().size() == number(1.0)"
        arg:
          IS_EQUAL:
            left:
              COLLECTION_SIZE:
                arg:
                  COLLECTION:
                    emptyValues: SKIP
                    duplicateValues: SKIP
                    ordering: SORTED
                    itemType: TEXT
                    items:
                      - "a"
                      - "A"
                      - " A "
                      - "\ta\n\r"
            right:
              NUMBER: 1
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required