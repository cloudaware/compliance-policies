---
inputType: CA10__CaGoogleCloudKmsCryptoKey__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        expected:
          status: UNDETERMINED
          subIndex: 1
          text: "''.isEmpty()"
        id: Test-1
        arg:
          IS_EMPTY:
            arg:
              DURATION_FROM:
                arg:
                  TEXT: ""
                format: GCP
                undeterminedIf:
                  isEmpty: "Empty text received"
                  invalidFormat: "Should not happen"
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          status: UNDETERMINED
          subIndex: 2
          text: "'123'.checkFormat('GCP')"
        arg:
          IS_EMPTY:
            arg:
              DURATION_FROM:
                arg:
                  TEXT: "123"
                format: GCP
                undeterminedIf:
                  isEmpty: "Should not happen"
                  invalidFormat: "Invalid value received"
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): '60s'.asDuration() == '1m'.asDuration()"
        arg:
          IS_EQUAL:
            left:
              DURATION_FROM:
                arg:
                  TEXT: "60s"
                format: GCP
                undeterminedIf:
                  isEmpty: "Should not happen"
                  invalidFormat: "Should not happen"
            right:
              DURATION_FROM:
                arg:
                  TEXT: "1m"
                format: GCP
                undeterminedIf:
                  isEmpty: "Should not happen"
                  invalidFormat: "Should not happen"
  - status: COMPLIANT
    currentStateMessage: Test-4
    check:
      UNIT_TEST:
        id: Test-4
        expected:
          text: "unitTest(Test-4): '60m'.asDuration() == '1h'.asDuration()"
        arg:
          IS_EQUAL:
            left:
              DURATION_FROM:
                arg:
                  TEXT: "60m"
                format: GCP
                undeterminedIf:
                  isEmpty: "Should not happen"
                  invalidFormat: "Should not happen"
            right:
              DURATION_FROM:
                arg:
                  TEXT: "1h"
                format: GCP
                undeterminedIf:
                  isEmpty: "Should not happen"
                  invalidFormat: "Should not happen"
  - status: COMPLIANT
    currentStateMessage: Test-5
    check:
      UNIT_TEST:
        id: Test-5
        expected:
          text: "unitTest(Test-5): '24h'.asDuration() == '1d'.asDuration()"
        arg:
          IS_EQUAL:
            left:
              DURATION_FROM:
                arg:
                  TEXT: "24h"
                format: GCP
                undeterminedIf:
                  isEmpty: "Should not happen"
                  invalidFormat: "Should not happen"
            right:
              DURATION_FROM:
                arg:
                  TEXT: "1d"
                format: GCP
                undeterminedIf:
                  isEmpty: "Should not happen"
                  invalidFormat: "Should not happen"  
  - status: COMPLIANT
    currentStateMessage: Test-6
    check:
      UNIT_TEST:
        id: Test-6
        expected:
          text: "unitTest(Test-6): '1s'.asDuration() == 'PT1S'"
        arg:
          IS_EQUAL:
            left:
              DURATION_FROM:
                arg:
                  TEXT: "1s"
                format: GCP
                undeterminedIf:
                  isEmpty: "Should not happen"
                  invalidFormat: "Should not happen"
            right:
              DURATION: "PT1S"
  - status: COMPLIANT
    currentStateMessage: Test-7
    check:
      UNIT_TEST:
        id: Test-7
        expected:
          text: "unitTest(Test-7): 'P1Y' != 'P365D'"
        arg:
          NOT_EQUAL:
            left:
              DURATION: "P1Y"
            right:
              DURATION: "P365D"
  - status: COMPLIANT
    currentStateMessage: Test-8
    check:
      UNIT_TEST:
        id: Test-8
        expected:
          text: "unitTest(Test-8): 'P1Y' == 'P12M'"
        arg:
          IS_EQUAL:
            left:
              DURATION: "P1Y"
            right:
              DURATION: "P12M"
  - status: COMPLIANT
    currentStateMessage: Test-9
    check:
      UNIT_TEST:
        id: Test-9
        expected:
          text: "unitTest(Test-9): 'P1M' == 'P30D'"
        arg:
          IS_EQUAL:
            left:
              DURATION: "P1M"
            right:
              DURATION: "P30D"
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required