---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
          text: "unitTest(Test-1): not(setOfText([]).containsAll(setOfText(['a'])))"
        arg:
          NOT:
            arg:
              CONTAINS_ALL:
                arg:
                  SET:
                    itemType: TEXT
                    items: []
                search:
                  SET:
                    itemType: TEXT
                    items:
                      - a
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): setOfText(['a']).containsAll(setOfText(['a', 'a']))"
        arg:
          CONTAINS_ALL:
            arg:
              SET:
                itemType: TEXT
                items:
                  - a
            search:
              SET:
                itemType: TEXT
                items:
                  - a
                  - a
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): not(setOfText(['a', 'b']).containsAll(setOfText(['a', 'c'])))"
        arg:
          NOT:
            arg:
              CONTAINS_ALL:
                arg:
                  SET:
                    itemType: TEXT
                    items:
                      - a
                      - b
                search:
                  SET:
                    itemType: TEXT
                    items:
                      - a
                      - c
  - status: COMPLIANT
    currentStateMessage: Test-4
    check:
      UNIT_TEST:
        id: Test-4
        expected:
          text: "unitTest(Test-4): not(null.containsAll(setOfText(['a'])))"
        arg:
          NOT:
            arg:
              CONTAINS_ALL:
                arg:
                  UNIT_TEST_NULL:
                    returnType: SET_OF_TEXT
                search:
                  SET:
                    itemType: TEXT
                    items:
                      - a
  - status: COMPLIANT
    currentStateMessage: Test-5
    check:
      UNIT_TEST:
        id: Test-5
        expected:
          text: "unitTest(Test-5): not(setOfText(['a']).containsAll(null))"
        arg:
          NOT:
            arg:
              CONTAINS_ALL:
                arg:
                  SET:
                    itemType: TEXT
                    items:
                      - a
                search:
                  UNIT_TEST_NULL:
                    returnType: SET_OF_TEXT
  - status: COMPLIANT
    currentStateMessage: Test-6
    check:
      UNIT_TEST:
        id: Test-6
        expected:
          text: "unitTest(Test-6): setOfText([' ABC ', ' De  f ']).containsAll(setOfText(['abc\\n\\t', 'de f']))"
        arg:
          CONTAINS_ALL:
            arg:
              SET:
                itemType: TEXT
                items:
                  - " ABC "
                  - " De  f "
            search:
              SET:
                itemType: TEXT
                items:
                  - "abc\n\t"
                  - "de f"
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required