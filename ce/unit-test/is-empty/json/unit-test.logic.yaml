---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
          text: "unitTest(Test-1): {\"name\":\"John\",\"age\":30}.isNotEmpty()"
        arg:
          NOT_EMPTY:
            arg:
              JSON: { name: "John", age: 30 }
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): {\"name\":\"John\"}.isNotEmpty()"
        arg:
          NOT_EMPTY:
            arg:
              JSON: { name: "John"}
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): {\"name\":null}.isEmpty()"
        arg:
          IS_EMPTY:
            arg:
              JSON: { name: null }
  - status: COMPLIANT
    currentStateMessage: Test-4
    check:
      UNIT_TEST:
        id: Test-4
        expected:
          text: "unitTest(Test-4): {}.isEmpty()"
        arg:
          IS_EMPTY:
            arg:
              JSON: { }
  - status: COMPLIANT
    currentStateMessage: Test-5
    check:
      UNIT_TEST:
        id: Test-5
        expected:
          text: "unitTest(Test-5): null.isEmpty()"
        arg:
          IS_EMPTY:
            arg:
              UNIT_TEST_NULL:
                returnType: TEXT
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required