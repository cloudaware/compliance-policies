---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
          text: "unitTest(Test-1): bytes('abc').isNotEmpty()"
        arg:
          NOT_EMPTY:
            arg:
              BYTES: "abc"
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): bytes(' ').isNotEmpty()"
        arg:
          NOT_EMPTY:
            arg:
              BYTES: " "
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): bytes('\\n\\r\\t').isNotEmpty()"
        arg:
          NOT_EMPTY:
            arg:
              BYTES: "\n\r\t"
  - status: COMPLIANT
    currentStateMessage: Test-4
    check:
      UNIT_TEST:
        id: Test-4
        expected:
          text: "unitTest(Test-4): bytes('').isEmpty()"
        arg:
          IS_EMPTY:
            arg:
              BYTES: ""
  - status: COMPLIANT
    currentStateMessage: Test-5
    check:
      UNIT_TEST:
        id: Test-5
        expected:
          text: "unitTest(Test-5): null.isEmpty()"
        arg:
          IS_EMPTY:
            arg:
              UNIT_TEST_NULL:
                returnType: BYTES
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required