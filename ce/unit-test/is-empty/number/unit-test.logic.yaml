---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
          text: "unitTest(Test-1): number(10.0).isNotEmpty()"
        arg:
          NOT_EMPTY:
            arg:
              NUMBER: 10.0
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): number(0.0).isNotEmpty()"
        arg:
          NOT_EMPTY:
            arg:
              NUMBER: 0
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): null.isEmpty()"
        arg:
          IS_EMPTY:
            arg:
              UNIT_TEST_NULL:
                returnType: NUMBER
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required