---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
          text: "unitTest(Test-1): true"
        arg:
          OR:
            args:
              - BOOLEAN: true
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): not(false)"
        arg:
          NOT:
            arg:
              OR:
                args:
                  - BOOLEAN: false
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): not(false || false)"
        arg:
          NOT:
            arg:
              OR:
                args:
                  - BOOLEAN: false
                  - BOOLEAN: false
  - status: COMPLIANT
    currentStateMessage: Test-4
    check:
      UNIT_TEST:
        id: Test-4
        expected:
          text: "unitTest(Test-4): true || false"
        arg:
          OR:
            args:
              - BOOLEAN: true
              - BOOLEAN: false
  - status: COMPLIANT
    currentStateMessage: Test-5
    check:
      UNIT_TEST:
        id: Test-5
        expected:
          text: "unitTest(Test-5): false || true"
        arg:
          OR:
            args:
              - BOOLEAN: false
              - BOOLEAN: true
  - status: COMPLIANT
    currentStateMessage: Test-6
    check:
      UNIT_TEST:
        id: Test-6
        expected:
          text: "unitTest(Test-6): true || true"
        arg:
          OR:
            args:
              - BOOLEAN: true
              - BOOLEAN: true
  - status: COMPLIANT
    currentStateMessage: Test-7
    check:
      UNIT_TEST:
        id: Test-7
        expected:
          text: "unitTest(Test-7): false || false || true"
        arg:
          OR:
            args:
              - BOOLEAN: false
              - BOOLEAN: false
              - BOOLEAN: true
  - status: COMPLIANT
    currentStateMessage: Test-8
    check:
      UNIT_TEST:
        id: Test-8
        expected:
          text: "unitTest(Test-8): true || error('Short-circuit evaluation: does not compute second argument')"
        arg:
          OR:
            args:
              - BOOLEAN: true
              - UNIT_TEST_RUNTIME_ERROR:
                  returnType: BOOLEAN
                  message: "Short-circuit evaluation: does not compute second argument"
  - status: COMPLIANT
    currentStateMessage: Test-9
    check:
      UNIT_TEST:
        id: Test-9
        expected:
          status: UNDETERMINED
          subIndex: 1
          text: "error('Short-circuit evaluation: compute second argument').throw()"
          runtimeError: "Short-circuit evaluation: compute second argument"
        arg:
          OR:
            args:
              - BOOLEAN: false
              - UNIT_TEST_RUNTIME_ERROR:
                  returnType: BOOLEAN
                  message: "Short-circuit evaluation: compute second argument"
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required