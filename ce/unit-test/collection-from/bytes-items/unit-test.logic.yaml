---
inputType: CA10__CaAwsInstance__c
conditions:
  - status: COMPLIANT
    currentStateMessage: Test-1
    check:
      UNIT_TEST:
        id: Test-1
        expected:
          text: "unitTest(Test-1): bytes('').asCollection(',').skipEmpty().skipDuplicates().sort().isEmpty()"
        arg:
          IS_EMPTY:
            arg:
              COLLECTION_FROM:
                separator: ","
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                arg:
                  BYTES: ""
  - status: COMPLIANT
    currentStateMessage: Test-1-Keep-Empty
    check:
      UNIT_TEST:
        id: Test-1-Keep-Empty
        expected:
          text: "unitTest(Test-1-Keep-Empty): bytes('').asCollection(',').keepEmpty().skipDuplicates().sort().isNotEmpty()"
        arg:
          NOT_EMPTY:
            arg:
              COLLECTION_FROM:
                separator: ","
                emptyValues: KEEP
                duplicateValues: SKIP
                ordering: SORTED
                arg:
                  BYTES: ""
  - status: COMPLIANT
    currentStateMessage: Test-2
    check:
      UNIT_TEST:
        id: Test-2
        expected:
          text: "unitTest(Test-2): bytes('a,a').asCollection(',').skipEmpty().skipDuplicates().sort() == ['a'].skipEmpty().skipDuplicates().sort()"
        arg:
          IS_EQUAL:
            left:
              COLLECTION_FROM:
                separator: ","
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                arg:
                  BYTES: "a,a"
            right:
              COLLECTION:
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                itemType: BYTES
                items:
                  - a
  - status: COMPLIANT
    currentStateMessage: Test-2-Keep-Duplicates
    check:
      UNIT_TEST:
        id: Test-2-Keep-Duplicates
        expected:
          text: "unitTest(Test-2-Keep-Duplicates): bytes('a,a').asCollection(',').skipEmpty().keepDuplicates().sort() != ['a'].skipEmpty().skipDuplicates().sort()"
        arg:
          NOT_EQUAL:
            left:
              COLLECTION_FROM:
                separator: ","
                emptyValues: SKIP
                duplicateValues: KEEP
                ordering: SORTED
                arg:
                  BYTES: "a,a"
            right:
              COLLECTION:
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                itemType: BYTES
                items:
                  - a
  - status: COMPLIANT
    currentStateMessage: Test-3
    check:
      UNIT_TEST:
        id: Test-3
        expected:
          text: "unitTest(Test-3): bytes('a,b').asCollection(',').skipEmpty().skipDuplicates().sort() == ['a', 'b'].skipEmpty().skipDuplicates().sort()"
        arg:
          IS_EQUAL:
            left:
              COLLECTION_FROM:
                separator: ","
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                arg:
                  BYTES: "a,b"
            right:
              COLLECTION:
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                itemType: BYTES
                items:
                  - a
                  - b
  - status: COMPLIANT
    currentStateMessage: Test-4
    check:
      UNIT_TEST:
        id: Test-4
        expected:
          text: "unitTest(Test-4): bytes(' a , a ').asCollection(',').skipEmpty().skipDuplicates().sort() == [' a '].skipEmpty().skipDuplicates().sort()"
        arg:
          IS_EQUAL:
            left:
              COLLECTION_FROM:
                separator: ","
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                arg:
                  BYTES: " a , a "
            right:
              COLLECTION:
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                itemType: BYTES
                items:
                  - " a "
  - status: COMPLIANT
    currentStateMessage: Test-4-Keep-Duplicates
    check:
      UNIT_TEST:
        id: Test-4-Keep-Duplicates
        expected:
          text: "unitTest(Test-4-Keep-Duplicates): bytes(' a , a ').asCollection(',').skipEmpty().skipDuplicates().sort() == [' a ', ' a '].skipEmpty().skipDuplicates().sort()"
        arg:
          IS_EQUAL:
            left:
              COLLECTION_FROM:
                separator: ","
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                arg:
                  BYTES: " a , a "
            right:
              COLLECTION:
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                itemType: BYTES
                items:
                  - " a "
                  - " a "
  - status: COMPLIANT
    currentStateMessage: Test-5
    check:
      UNIT_TEST:
        id: Test-5
        expected:
          text: "unitTest(Test-5): bytes('\\nline1\\nline2\\nline3\\n').asCollection('\\n').skipEmpty().skipDuplicates().sort() == ['line3', 'line2', 'line1'].skipEmpty().skipDuplicates().sort()"
        arg:
          IS_EQUAL:
            left:
              COLLECTION_FROM:
                separator: "\n"
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                arg:
                  BYTES: "\nline1\nline2\nline3\n"
            right:
              COLLECTION:
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                itemType: BYTES
                items:
                  - line3
                  - line2
                  - line1
  - status: COMPLIANT
    currentStateMessage: Test-5-Original-Order
    check:
      UNIT_TEST:
        id: Test-5-Original-Order
        expected:
          text: "unitTest(Test-5-Original-Order): bytes('\\nline1\\nline2\\nline3\\n').asCollection('\\n').skipEmpty().skipDuplicates().sort() != ['line3', 'line2', 'line1'].skipEmpty().skipDuplicates().originalOrder()"
        arg:
          NOT_EQUAL:
            left:
              COLLECTION_FROM:
                separator: "\n"
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                arg:
                  BYTES: "\nline1\nline2\nline3\n"
            right:
              COLLECTION:
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: ORIGINAL
                itemType: BYTES
                items:
                  - line3
                  - line2
                  - line1
  - status: COMPLIANT
    currentStateMessage: Test-6
    check:
      UNIT_TEST:
        id: Test-6
        expected:
          text: "unitTest(Test-6): [' ABC '].skipEmpty().skipDuplicates().sort().contains(bytes(' ABC '))"
        arg:
          CONTAINS:
            arg:
              COLLECTION:
                emptyValues: SKIP
                duplicateValues: SKIP
                ordering: SORTED
                itemType: BYTES
                items:
                  - " ABC "
            search:
              BYTES: " ABC "
otherwise:
  status: INCOMPLIANT
  currentStateMessage: Test Failed
  remediationMessage: Developer Fix Required