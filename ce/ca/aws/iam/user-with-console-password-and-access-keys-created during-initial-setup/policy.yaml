---
names:
  full: AWS IAM User with a console password has access keys created during the initial
    setup.
  contextual: AWS IAM User with a console password has access keys created during
    the initial setup.
description: "AWS console defaults to no check boxes selected when creating a new\
  \ IAM user. When creating the IAM User credentials you have to determine what type\
  \ of access they require. Programmatic access: The IAM user might need to make API\
  \ calls, use the AWS CLI, or use the Tools for Windows PowerShell. In that case,\
  \ create an access key (access key ID and a secret access key) for that user. AWS\
  \ Management Console access: If the user needs to access the AWS Management Console,\
  \ create a password for the user."
impossible: true
type: COMPLIANCE_POLICY
categories:
  - SECURITY
frameworkMappings:
  - /frameworks/cis-aws-v3.0.0/01/11
  - /frameworks/cloudaware/identity-and-access-management/credentials-controls
similarPolicies:
  internal:
    - dec-x-b10e98af
  cloudConformity:
    - url: https://www.trendmicro.com/cloudoneconformity/knowledge-base/aws/IAM/access-keys-during-initial-iam-user-setup.html
      name: Access Keys During Initial IAM User Setup
