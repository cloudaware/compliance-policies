---
name: "3.1.1 Ensure that the kubeconfig file permissions are set to 644 or \
  \ more restrictive (Automated)"
description: "If `kubelet` is running, and if it is configured by a kubeconfig file, ensure that the proxy \
  \ kubeconfig file has permissions of 644 or more restrictive."
similar:
  sections:
    - "/frameworks/cis-gke-v1.0.0/04/01/05"