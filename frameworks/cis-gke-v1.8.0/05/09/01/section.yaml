---
name: "5.9.1 Enable Customer-Managed Encryption Keys (CMEK) for \
  \ GKE Persistent Disks (PD) (Manual)"
description: "Use Customer-Managed Encryption Keys (CMEK) to encrypt dynamically-provisioned \
  \ attached Google Compute Engine Persistent Disks (PDs) using keys managed within \
  \ Cloud Key Management Service (Cloud KMS)."
similar:
  sections:
    - "/frameworks/cis-gke-v1.0.0/06/09/01"