---
name: "4.1.1 Ensure that the cluster-admin role is only used where \
  \ required (Automated)"
description: "The RBAC role `cluster-admin` provides wide-ranging powers over the environment \
  \ and should be used only where and when needed."
similar:
  sections:
    - "/frameworks/cis-gke-v1.0.0/05/01/01"