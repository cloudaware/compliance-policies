---
name: "4.2.1 Ensure that the cluster enforces Pod Security Standard \
  \ Baseline profile or stricter for all namespaces. (Manual)"
description: "The Pod Security Standard Baseline profile defines a baseline for container security. \
  \ You can enforce this by using the built-in Pod Security Admission controller."
