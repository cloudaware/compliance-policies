---
name: "4.6.2 Ensure that the seccomp profile is set to RuntimeDefault in \
  \ the pod definitions (Automated)"
description: "Enable `RuntimeDefault` seccomp profile in the pod definitions."
similar:
  sections:
    - "/frameworks/cis-gke-v1.0.0/05/06/02"