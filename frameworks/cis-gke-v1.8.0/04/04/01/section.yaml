---
name: "4.4.1 Prefer using secrets as files over secrets as environment \
  \ variables (Automated)"
description: "Kubernetes supports mounting secrets as data volumes or as environment variables. \
  \ Minimize the use of environment variable secrets."
similar:
  sections:
    - "/frameworks/cis-gke-v1.0.0/05/04/01"