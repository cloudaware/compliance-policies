---
name: 1.14 Ensure API Keys Are Restricted to Only APIs That Application Needs Access
  - Level 1 (Manual)
description: "API keys are always at risk because they can be viewed publicly, such\
  \ as from within a browser, or they can be accessed on a device where the key resides.\
  \ It is recommended to restrict API keys to use (call) only APIs required by an\
  \ application."
similar:
  sections:
    - /frameworks/cis-gcp-v2.0.0/01/14
    - /frameworks/cis-gcp-v1.2.0/01/14
  internal:
    - dec-c-82b526de
