---
name: 6.2.2 Ensure That the ‘Log_connections’ Database Flag for Cloud SQL PostgreSQL
  Instance Is Set to ‘On’ - Level 1 (Automated)
description: "Enabling the `log_connections` setting causes each attempted connection\
  \ to the server to be logged, along with successful completion of client authentication.\
  \ This parameter cannot be changed after the session starts."
similar:
  internal:
    - dec-c-cf5ba169
