---
name: 2.2.2.2 Lock devices after more than 10 unsuccessful attempts.
description: |-
  When it's possible to configure, you should apply locking devices after more than 10 unsuccessful attempts.
  When the vendor doesn't allow you to configure the above, use the vendor’s default setting.
similar:
  internal:
    - dec-c-22e0b09d
