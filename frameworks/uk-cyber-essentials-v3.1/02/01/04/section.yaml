---
name: 2.1.4 Disable any auto-run feature which allows file execution without user authorization
description: |-
  Disable any auto-run feature which allows file execution without user authorisation (such as when they are downloaded)
similar:
  internal:
    - dec-c-db8fa560
