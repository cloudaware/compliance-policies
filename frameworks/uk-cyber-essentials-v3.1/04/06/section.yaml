---
name: 4.6 Remove or disable special access privileges when no longer required
description: |-
  Remove or disable special access privileges when no longer required (when a member of staff changes role, for example)
similar:
  internal:
    - dec-c-dc443f2d
