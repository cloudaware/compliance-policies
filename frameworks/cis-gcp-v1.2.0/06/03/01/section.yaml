---
name: 6.3.1 Ensure 'external scripts enabled' database flag for Cloud SQL SQL Server
  instance is set to 'off' - Level 1 (Automated)
description: It is recommended to set `external scripts enabled` database flag for
  Cloud SQL SQL Server instance to `off` Will be supported in the near future.
similar:
  sections:
    - /frameworks/cis-gcp-v1.3.0/06/03/01
  internal:
    - dec-c-643f19e7
