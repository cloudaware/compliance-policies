---
name: 6.3.3 Ensure 'user connections' database flag for Cloud SQL SQL Server instance
  is set as appropriate - Level 1 (Automated)
description: It is recommended to set `user connections` database flag for Cloud SQL
  SQL Server instance according organization-defined value. Will be supported in the
  near future.
similar:
  internal:
    - dec-c-dd81abbf
