---
name: 6.3.7 Ensure that the 'contained database authentication' database flag for
  Cloud SQL on the SQL Server instance is set to 'off' - Level 1 (Automated)
description: It is recommended to set `contained database authentication` database
  flag for Cloud SQL on the SQL Server instance is set to `off`.
similar:
  sections:
    - /frameworks/cis-gcp-v1.3.0/06/03/07
  internal:
    - dec-c-be6b74bc
