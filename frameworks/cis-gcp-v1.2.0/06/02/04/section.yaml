---
name: 6.2.4 Ensure that the 'log_disconnections' database flag for Cloud SQL PostgreSQL
  instance is set to 'on' - Level 1 (Automated)
description: "Enabling the `log_disconnections` setting logs the end of each session,\
  \ including the session duration."
similar:
  sections:
    - /frameworks/cis-gcp-v1.3.0/06/02/03
  internal:
    - dec-c-c90a8993
