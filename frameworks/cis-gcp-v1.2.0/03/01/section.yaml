---
name: 3.1 Ensure that the default network does not exist in a project - Level 2 (Automated)
description: "To prevent use of `default` network, a project should not have a `default`\
  \ network."
similar:
  sections:
    - /frameworks/cis-gcp-v1.3.0/03/01
  internal:
    - dec-c-f59bb56b
