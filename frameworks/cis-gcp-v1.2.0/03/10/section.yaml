---
name: "3.10 Ensure Firewall Rules for instances behind Identity Aware Proxy (IAP)\
  \ only allow the traffic from Google Cloud Loadbalancer (GCLB) Health Check and\
  \ Proxy Addresses - Level 2 (Manual | Not supported, requires a manual assessment)"
description: Access to VMs should be restricted by firewall rules that allow only
  IAP traffic by ensuring only connections proxied by the IAP are allowed. To ensure
  that load balancing works correctly health checks should also be allowed.
similar:
  internal:
    - dec-c-5f856141
