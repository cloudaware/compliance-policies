---
name: "4.1.9 Ensure that the kubelet configuration file has permissions set to \
  \ 644 or more restrictive (Scored)"
description: "Ensure that if the kubelet refers to a configuration file with the `--config` argument, that file \
  \ has permissions of 644 or more restrictive."
