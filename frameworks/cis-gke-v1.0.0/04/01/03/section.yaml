---
name: "4.1.3 Ensure that the proxy kubeconfig file permissions are set to 644 or \
  \ more restrictive (Scored)"
description: "If `kube-proxy` is running, and if it is using a file-based kubeconfig file, ensure that the proxy \
  \ kubeconfig file has permissions of `644` or more restrictive."
