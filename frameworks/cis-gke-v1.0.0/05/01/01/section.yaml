---
name: "5.1.1 Ensure that the cluster-admin role is only used where required (Not Scored)"
description: "The RBAC role `cluster-admin` provides wide-ranging powers over the environment and \
  \ should be used only where and when needed."
