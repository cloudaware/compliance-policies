---
name: "5.2.5 Minimize the admission of containers with allowPrivilegeEscalation (Scored)"
description: "Do not generally permit containers to be run with the `allowPrivilegeEscalation` flag \
  \ set to `true`."
