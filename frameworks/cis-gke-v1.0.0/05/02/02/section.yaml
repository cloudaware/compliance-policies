---
name: "5.2.2 Minimize the admission of containers wishing to share the host \
  \ process ID namespace (Scored)"
description: "Do not generally permit containers to be run with the `hostPID` flag set to `true`."
