---
name: "5.2.4 Minimize the admission of containers wishing to share the host \
  \ network namespace (Scored)"
description: "Do not generally permit containers to be run with the `hostNetwork` flag set to `true`."
