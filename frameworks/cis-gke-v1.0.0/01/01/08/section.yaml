---
name: "1.1.8 Ensure that the etcd pod specification file ownership is set to \
  \ root:root (Not Scored)"
description: "Ensure that the /etc/kubernetes/manifests/etcd.yaml file ownership is set to \
  \ root:root."
