---
name: "1.1.6 Ensure that the scheduler pod specification file ownership is set to \
  \ root:root (Not Scored)"
description: "Ensure that the scheduler pod specification file ownership is set to \
  \ root:root."
