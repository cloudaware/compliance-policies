---
name: >
  [ElastiCache.4] ElastiCache replication groups should be encrypted at rest
description: |-
  Encrypting data at rest reduces the risk that an unauthenticated user gets access 
  to data that is stored on disk. ElastiCache (Redis OSS) replication groups should 
  be encrypted at rest for an added layer of security.
similar:
  internal:
    - dec-c-0bfb2c2f
  awsSecurityHub:
    controlUrl: https://docs.aws.amazon.com/securityhub/latest/userguide/elasticache-controls.html#elasticache-4