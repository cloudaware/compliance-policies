---
name: 4.2 Ensure that 'AuditActionGroups' in 'auditing' policy for a SQL server is
  set properly
description: "Configure the 'AuditActionGroups' property to appropriate groups to\
  \ capture all the critical activities on the SQL Server and all the \nSQL databases\
  \ hosted on the SQL server."
similar:
  internal:
    - dec-c-579d6f1b
