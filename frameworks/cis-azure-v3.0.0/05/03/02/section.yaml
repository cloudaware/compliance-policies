---
name: "5.3.2 Ensure server parameter 'tls_version' is set to 'TLSv1.2' (or higher) for MySQL flexible server (Automated)"
description: >
  Ensure `tls_version` on `MySQL flexible servers` is set to use TLS version 1.2 or higher.
