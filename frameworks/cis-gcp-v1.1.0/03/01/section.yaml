---
name: 3.1 Ensure that the default network does not exist in a project
description: "To prevent use of 'default' network, a project should not have a 'default'\
  \ network."
similar:
  internal:
    - dec-c-80fc838f
